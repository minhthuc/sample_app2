<%= form_for want_follow(current_user), remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, folow_user.id %></div>
  <%= f.submit t("users.follow"), class: "btn btn-primary" %>
<% end %>
